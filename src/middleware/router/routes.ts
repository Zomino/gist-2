export enum AuthRoutes {
  BASE = '/auth',
  LOGIN = '/login',
  LOGIN_RETURN = '/login/return',
  LOGOUT = '/logout',
}

export enum HomeRoutes {
  BASE = '/',
}

export enum LoginRoutes {
  BASE = '/login',
}

export default {
  AuthRoutes,
  HomeRoutes,
  LoginRoutes,
};
